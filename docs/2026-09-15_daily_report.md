# Delta 项目日报与续接指南 — 2026-09-15

今天完成了第一次 Delta 实际佩戴外出的数据核对，并把设备的满存策略改成
“优先保留最新记录”。网站的活动摘要也已改成更保守的观察标签，避免把尚未
用犬只真实数据标定的阈值误说成“走路／跑步识别”。

## 今天的结论

| 项目 | 状态 | 可复核结果 |
|---|---|---|
| 首次 Delta 佩戴外出 | **数据已收到** | 网站已有两段相邻记录：9 月 14 日 21:13:17（3 分 02 秒、3,586 条、正常结束）和 21:17:04（3 分 24 秒、4,018 条） |
| 第二段的文件完整性 | **通过** | 网站保存了完整原始文件；4,018 条有效样本，19.7 Hz，采样间隙 4.2 秒（跨度的 2.1%） |
| 第二段的结束原因 | 待结合现场确认 | 记录内电压 3.30 → 3.13 V，且没有写入正常结束标记；这与低电压／掉电相符，但不把它当作已由硬件完全证明的结论 |
| 充电复核 | 正在恢复 | 外置开关打开后，实测工作电压曾由约 3.87 V 上升至约 3.96 V；CHG 指示灯亮表示正在充电，不能单独证明已充满 |
| 私有网站 | 已更新并发布 | 活动呈现改为观察性“静止／活动／持续高活动”；低电压提示和相邻分段汇总已加入，站点仍仅所有者可见 |
| 本地固件的最新记录保留 | 代码和主机模拟通过 | 满存时先删除已被网站确认的最旧记录；仍不足才删除最旧的仅本地旧分段；当前写入的分段绝不删除 |
| 板子固件 | **尚未刷写** | 本次只完成代码与主机验证，未重启、未擦除、未改动 Delta 板内的现有记录 |

## 外出数据：没有白费

两段记录合计 **6 分 26 秒、7,604 条样本**。两段之间约有 45 秒间隔，
因此不能视作严格连续的一条轨迹；但每一段实际到达网站的数据都有效。第一段
正常结束；第二段虽标为“结束待确认”，网站的完整性校验通过，表示原始文件
不是因传输而丢失或损坏。

第二段结束时的 3.13 V 已明显偏低，结合当时设备中途停止的现象，低电压是目前
最合理的解释。下一次外出前，应在外置开关**打开**的状态下继续充电至 CHG 熄灭
或电池工作电压稳定在约 4.1–4.2 V；充电仍须有人看护，若电池异常发热、鼓包或
有异味则停止使用。

## 容量与新的满存策略

`motion` 分区为 **4 MiB**；每个 IMU 样本为 24 字节，20 Hz 时理论上一小时
原始数据约为 1.73 MB。实际长记录已经测到单段 **150,289 条、130 分 38 秒**，
这比公式更可信地代表当前文件系统和采样间隔下的容量边界。

原来的行为是：已上传的记录可在空间紧张时清理，但如果设备长时间离开家中
Wi-Fi、所有旧记录都尚未上传，记录会最终因存储满而停止。现在的固件改为：

1. 空间需要时，先清理网站已以 SHA-256 确认收到的最旧文件；网站上的副本保留。
2. 若仍不足，当前段会先正常收尾，再新开一段；仅删除最旧的、尚未上传的
   本地旧段，给新段让出空间。
3. 正在写入的当前文件和刚结束、最新的那一段都受保护，长途记录会成为若干
   分段，设备尽可能保留最近的部分。

这是明确的数据取舍：**极长时间不回家同步时，最早的未上传分段可能会被淘汰，
以换取最新的记录继续保存。** 在能回家同步的正常流程中，优先删除的是已有
云端完整副本的历史记录。

实现位于 [cloud_sync.cpp](../firmware/imu_raw_stream/src/cloud_sync.cpp) 和
[cloud_sync.h](../firmware/imu_raw_stream/src/cloud_sync.h)；手动开始、自动离家开始
和家中活动开始均使用同一套空间策略。本地 `/records` 页会显示本次开机清理数，
并区分其中有多少是“仅本地旧段”。

## 网站分析的调整

私有网站已发布新的观察规则：

- 静止阈值：低于 **50 mg**；
- 高活动阈值：高于 **600 mg**；
- 只有连续 **6 秒**的高活动才显示“持续高活动”；短暂峰值只作为短时高活动候选；
- 不再把这些当前阈值直接命名为犬只“走路／跑步”。

这些是展示和后续校准的起点，不是医学指标，也不是已由 Delta 真实佩戴数据验证
的步态分类。下一次在充分充电后取得更长的佩戴样本，才能据此调整阈值。

## 验证和已知边界

- `sh firmware/imu_raw_stream/tests/cloud_sync_sim/run.sh`：通过。该主机模拟验证
  了“已确认记录优先”，以及仅在显式启用后才会移除最旧的未上传闭合分段；活动
  文件不会被删除。
- `python3 firmware/imu_raw_stream/tests/test_offline_log.py`：通过。它检验一小时的
  二进制编码、CRC、批量写入和部分批次收尾。
- `git diff --check`：通过，无空白错误。
- 完整的 PlatformIO 目标板编译／上传还没有执行：此工作区当前没有可调用的
  `pio`／`platformio` 命令。因此代码尚未生效到硬件。刷写时应只做普通固件上传，
  **不要**擦除 flash 或上传文件系统，以保护已有运动记录。

## 下次如何继续

1. 若要启用“最新记录保留”，先在 USB 已连接、Delta 未录制时完成一次普通固件
   编译和上传；上传会重启设备，但不应使用擦除 flash 的流程。
2. 让设备在外置开关打开时充至完成；拔 USB 后等待正常启动，再进行短时间、
   有人看护的佩戴测试。
3. 回到家中 Wi-Fi 后保持设备开机，等待自动结束和上传；在网站确认文件完整后
   再接 USB 充电。
4. 以多个真实佩戴片段复核“静止／活动／持续高活动”阈值；不要基于一次短测
   把它解释为可靠的跑步或健康诊断。

## 续接入口

- 本日报：[2026-09-15_daily_report.md](2026-09-15_daily_report.md)
- 长电池记录容量依据：[battery_recorded_long_2026-09-13.md](../experiments/battery_recorded_long_2026-09-13.md)
- 自动回家同步设计：[2026-09-11_home_wifi_cloud_sync.md](2026-09-11_home_wifi_cloud_sync.md)
- 设备固件说明：[firmware/imu_raw_stream/README.md](../firmware/imu_raw_stream/README.md)
- 私有网站：[Delta 散步记录](https://delta-walk-jz-20260911.yesmora.chatgpt.site/)
