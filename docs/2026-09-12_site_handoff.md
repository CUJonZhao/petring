# 网站续接 — 2026-09-12

网站分析更新已发布成功，仍仅所有者可访问：
https://delta-walk-jz-20260911.yesmora.chatgpt.site

网站独立仓库备份：https://github.com/CUJonZhao/petring-site （private）。
本地 site/ 的 github 远端用于备份；Sites 源仓库用于发布，二者已推送同一提交。
网站提交：9d5703e2dda64589cd0b3f484a4d7aaa0ae57dc0，Sites version 2。

## 本次变更

- 上线昨晚的活动分析与室内手持阈值。
- 主指标采用同一套窗口分析的静止／走／跑时间；设备原始活动曲线独立标注。
- 区分采样间隙与分析窗口未分类时间，明确跑动阈值未标定、犬类佩戴未验证。
- 详情加载失败显示独立重试入口；列表自动刷新不会清除详情错误。
- 旧记录详情以当前重新计算的 summary 覆盖旧元数据；列表移除可能过时的行为汇总。
- 更新运动触发和存储说明。

## 验证与限制

TypeScript 类型检查、生产构建通过。本地已有测试记录页面可打开并显示新文案。
在本次评估中，用 ce940dee 原始记录重算：静止 180 秒、走 162 秒、跑 0 秒、活动指数 80 mg。
该记录原始采样间隙为 8.728 秒；窗口未分类为 2 秒，两者不是同一个指标。
Sites 返回部署 succeeded。线上登录后的记录与下载尚未复核，浏览器缺少登录会话。
未刷写设备，未进行新的实机或电池测试。

## 下一步

1. 登录网站，核对 ce940dee 的上述分析值与下载。
2. 一小时电池独立记录与回家上传验证。
3. 用户恢复佩戴测试后，用标注的胸背记录重新标定犬类静止／走／跑。

环境：当前 bundled Node 可运行项目的 scripts/run-framework.mjs build；登录 shell 没有 npm。
Sites 插件缓存路径在本次工作中发生变化，打包使用当时可用的 bundled sites/package-site.sh。
密钥和原始记录继续只保留在忽略目录，不在 GitHub 网站源码备份中。
